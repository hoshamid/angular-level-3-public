import { DropdownOptionsPipe } from './dropdownoptions.pipe';

describe('DropdownoptionPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
